<h1>Songs</h1>

<!-- "Daft Punk - The Grid", href: song_path(@song) -->

<ul>
  <% @songs.each do |song| %>
     <li> <%= link_to "#{song.artist_name} - #{song.title}", song_path(song) %></li>
  <% end %>
</ul>
